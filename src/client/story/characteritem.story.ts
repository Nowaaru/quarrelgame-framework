import CharacterItem, { CharacterItemProps } from "client/ui/characterselect/characteritem";
import { story } from "shared/util/story";

export = story<CharacterItemProps>({
    component: CharacterItem
})